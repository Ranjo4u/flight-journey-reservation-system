# Group7 Flight Journey Reservation System (CLI)

Python CLI application for searching flights, booking journeys, and mock payments.

## Run
```bash
python main.py
```

## Data
Text-based storage under `data/` using JSONL (one JSON object per line).

